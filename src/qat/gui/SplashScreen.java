package qat.gui;/** * * @author webhiker * @version 2.3, 17 June 1999 */import java.awt.*;import javax.swing.border.*;import javax.swing.*;public class SplashScreen extends JWindow {	private JLabel statusLabel;	private JLabel splashImage;	private JProgressBar gauge;	public SplashScreen(JFrame parent, ImageIcon image, String label) {		super(parent);		Color background = new Color(66,66,99);		Color foreground = Color.white;		JPanel main = new JPanel(new BorderLayout());				main.setBorder(BorderFactory.createLineBorder(Color.black));		main.setBackground(background);		// set up the splash panel components		main.add(splashImage = new JLabel(image),BorderLayout.CENTER);		splashImage.setBackground(background);		JPanel temp = new JPanel(new BorderLayout());		temp.add(statusLabel = new JLabel(label,SwingConstants.CENTER),BorderLayout.NORTH);		statusLabel.setForeground(foreground);		temp.add(gauge = new JProgressBar(),BorderLayout.SOUTH);		main.add(temp,BorderLayout.SOUTH);		gauge.setMaximum(100);		getContentPane().add(main);		getContentPane().setBackground(background);		setBackground(background);		pack();		// centre the window		setLocation((Toolkit.getDefaultToolkit().getScreenSize().width/2)-(getSize().width/2),					(Toolkit.getDefaultToolkit().getScreenSize().height/2)-(getSize().height/2));	}    	public void showSplash() {		setVisible(true);	}  	public void hideSplash() {		setVisible(false);		dispose();	}  	public void setText(String text) {		statusLabel.setText(text);	}		public void setText(String text, int gaugeVal) {		statusLabel.setText(text);		gauge.setValue(gaugeVal);	}  	public String getText() {		return statusLabel.getText();	}}