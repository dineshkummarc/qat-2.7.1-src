package qat.gui;/** * * @author webhiker * @version 2.3, 17 June 1999 */import java.awt.event.*;import java.awt.image.*;import java.awt.*;import javax.swing.*;import java.net.*;import qat.common.Common;import qat.common.Resources;public class AboutDialog extends JDialog implements ActionListener {	    public AboutDialog(JFrame parent) {	super(parent,"About QA Tester");	JPanel center = new JPanel(new GridLayout(1,1));	JPanel splash = new JPanel(new BorderLayout());	JButton l;	splash.add(l=new JButton("http://qat.sourceforge.net", 				 new ImageIcon(Resources.getResource(Resources.getString("splashImage")))),		   BorderLayout.CENTER);	l.setFocusPainted(false);	Color background = new Color(148,148,181);	l.setBackground(background);	l.setForeground(Color.white);	splash.setBackground(background);	l.setVerticalTextPosition(SwingConstants.BOTTOM);l.setHorizontalTextPosition(SwingConstants.CENTER);	l.addActionListener(this);	center.add(splash);	JPanel south = new JPanel();	JButton okButton = new JButton("Close");	JTextArea descr = new JTextArea("Qat;khat;kat - a natural stimulant from the\nCatha Edulis plant, structurally and chemically\nsimilar to d-amphetamine.");	south.add(descr);	south.add(okButton);	descr.setBackground(okButton.getBackground());	descr.setEditable(false);	getContentPane().add(new JLabel("QAT V"+Common.VERSION,					SwingConstants.CENTER),			     BorderLayout.NORTH);	getContentPane().add(center,BorderLayout.CENTER);	getContentPane().add(south,BorderLayout.SOUTH);	okButton.addActionListener(new ActionListener() {		public void actionPerformed(ActionEvent evt) {setVisible(false);} });	addWindowListener(new WindowAdapter() {		public void windowClosing(WindowEvent e) { setVisible(false); } });	pack();	setResizable(false);	// now centre this window in the middle of the parent	setLocationRelativeTo(parent);	setVisible(true);    }	    public void actionPerformed(ActionEvent e) {	((JButton)e.getSource()).setIcon(new ImageIcon(Resources.getResource(Resources.getString("steveImage"))));	((JButton)e.getSource()).setBackground(Color.black);    }}